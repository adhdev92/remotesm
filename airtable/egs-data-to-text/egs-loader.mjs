/** @typedef {{title?:string,freebieStart?:string,freebieEnd?:string}} Promo */
/** @typedef {{kind:string,strength:number,sentence:string,data:object}} Insight */
/** @param {Promo[]} rows @param {{referenceDate:string,target?:'active'|'next'|'startingOn',milestoneStep?:number}} o @returns {{targets:Promo[],insights:Insight[]}} */
export function ordinalInsights(rows,o){rows=rows.filter(r=>r.freebieStart).sort((a,b)=>a.freebieStart.localeCompare(b.freebieStart));const t=pick(rows,o.referenceDate,o.target??'next');if(!t.length)return{targets:[],insights:[]};const d=t[0].freebieStart,y=d.slice(0,4),m=d.slice(0,7),b=rows.filter(r=>r.freebieStart<d),n=t.length,f=b.length+1,l=b.length+n,mb=b.filter(r=>r.freebieStart.startsWith(m)).length+1,yb=b.filter(r=>r.freebieStart.startsWith(y)).length+1,step=o.milestoneStep??50,hit=Math.ceil(f/step)*step<=l?Math.ceil(f/step)*step:null;return{targets:t,insights:[I('monthOrdinal',.72,line(t,mb,mb+n-1,month(d)),{first:mb,last:mb+n-1}),I('yearOrdinal',.78,line(t,yb,yb+n-1,y),{first:yb,last:yb+n-1}),I('overallOrdinal',hit?.97:.35,line(t,f,l,'the program overall'),{first:f,last:l,milestone:hit})]};}
function pick(r,d,m){if(m==='startingOn')return r.filter(x=>x.freebieStart===d);if(m==='active')return r.filter(x=>x.freebieStart<=d&&(!x.freebieEnd||d<x.freebieEnd));const n=r.find(x=>x.freebieStart>d)?.freebieStart;return n?r.filter(x=>x.freebieStart===n):[]}function line(r,a,b,p){const s=r.length===1?r[0].title:`These ${r.length} freebies`;return a===b?`${s} ${r.length===1?'is':'are'} the ${ord(a)} freebie ${p==='the program overall'?'in':'to begin in'} ${p}.`:`${s} occupy the ${ord(a)} through ${ord(b)} freebie slots ${p==='the program overall'?'in':'to begin in'} ${p}.`}function ord(n){const x=n%100;return`${n}${x>10&&x<14?'th':n%10===1?'st':n%10===2?'nd':n%10===3?'rd':'th'}`}function month(d){return new Intl.DateTimeFormat('en-US',{month:'long',year:'numeric',timeZone:'UTC'}).format(new Date(`${d}T00:00:00Z`))}function I(kind,strength,sentence,data){return{kind,strength,sentence,data}}export default ordinalInsights;
